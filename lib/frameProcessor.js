export function processFrame(canvas) {
  // In a real application, this function would send the canvas
  // to an AI model for analysis. For this example, we'll just log it.
  console.log('Processing frame:', canvas.width + 'x' + canvas.height);
}
